package com.rsi.agp.dao.tables.poliza;

import java.util.ArrayList;
import java.util.List;

import com.rsi.agp.core.webapp.util.FilaCompVinculada;
// Generated by T-Systems
import com.rsi.agp.core.webapp.util.StringUtils;


public class VistaComparativas  implements java.io.Serializable {

     private VistaComparativasId id;
     private FilaCompVinculada filavinculada;
     private List <VistaComparativas> filasVinculadas;
     
    public VistaComparativas() {
    	filasVinculadas = new ArrayList<VistaComparativas>(0);
    }

    public VistaComparativas(VistaComparativasId id) {
       this.id = id;
       filasVinculadas = new ArrayList<VistaComparativas>(0);
    }
   
    public VistaComparativasId getId() {
        return this.id;
    }
    
    public void setId(VistaComparativasId id) {
        this.id = id;
    }

	public FilaCompVinculada getFilavinculada() {
		return filavinculada;
	}

	public void setFilavinculada(FilaCompVinculada filavinculada) {
		this.filavinculada = filavinculada;
	}

	public List<VistaComparativas> getFilasVinculadas() {
		return filasVinculadas;
	}

	public void setFilasVinculadas(List<VistaComparativas> filasVinculadas) {
		this.filasVinculadas = filasVinculadas;
	}
	
	/*
     * Método que devuelve un Long formado por el codmodulo,Codconceptoppalmod,Codriesgocubierto y Filamodulo
     */
	public Double getclaveComparacion(){
    	Double res = null;
    	if (this.getId().getCodmodulo() != null && this.getId().getCodconceptoppalmod() != null && this.getId().getCodriesgocubierto() != null && this.getId().getFilamodulo() != null){
    		//String mod = String.format("%05d", new Integer(this.getId().getCodmodulo().toString()));
    		String cpm = String.format("%03d", new Integer(this.getId().getCodconceptoppalmod().toString()));
    		String rc  = String.format("%03d", new Integer(this.getId().getCodriesgocubierto().toString()));
    		String fm  = String.format("%03d", new Integer(this.getId().getFilamodulo().toString()));
    		
    		Long modAscii = StringUtils.convertirModuloParaOrdenacion(this.getId().getCodmodulo().toString());
			String clave1=	modAscii +	cpm + rc + fm;
			res = new Double(clave1);
    	}
    	return res;
    }

}